# Copy this file to local.properties and replace the example values.
# local.properties is ignored by Git and must never be committed.
sdk.dir=/path/to/Android/sdk
GEMINI_API_KEY=INTRODUCE_AQUI_TU_CLAVE
